@model List<Users>

@if (Model.Count == 0)
{
    <p>No users found for the selected group.</p>
}
else
{
    <ul>
        @foreach (var user in Model)
        {
            <li>@user.Name</li>
        }
    </ul>
}